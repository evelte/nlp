from setuptools import setup

setup(name='nlp',
      version=version,
      url='http://nlp.org',
      description='',
      author='',
      license='',
      packages=[],
      zip_safe=False,
      install_requires=[])
