from setuptools import setup

setup(name='nlp_new',
      version=version,
      url='http://nlp_new.org',
      description='',
      author='',
      license='',
      packages=[],
      zip_safe=False,
      install_requires=[])
